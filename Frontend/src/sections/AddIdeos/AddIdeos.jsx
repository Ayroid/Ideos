import styles from "./AddIdeos.module.css";

const { mainDiv, addIdeosForm } = styles;

const AddIdeos = () => {
  return (
    <div className={mainDiv}>
      <div className={addIdeosForm}>fd</div>
    </div>
  );
};

export default AddIdeos;
